meta {
  name: getUsers
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/security/users/
  body: none
  auth: bearer
}

params:query {
  ~roles.roleType: Support
}

auth:bearer {
  token: {{token}}
}
