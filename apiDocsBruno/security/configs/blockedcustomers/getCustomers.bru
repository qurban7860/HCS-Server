meta {
  name: getCustomers
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/security/configs/blockedcustomers/
  body: json
  auth: bearer
}

params:query {
  ~name: Naveed Ilyas
}

auth:bearer {
  token: {{token}}
}
