meta {
  name: post Ticket Change Type
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/tickets/settings/changeTypes/
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "icon": "mdi:type",
    "name": "Review",
    "slug": "slug",
    "description": "description!",
    "displayOrderNo": "1",
    "isDefault": true
  }
}
