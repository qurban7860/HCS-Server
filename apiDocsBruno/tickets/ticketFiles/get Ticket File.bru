meta {
  name: get Ticket File
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/tickets/:ticketId/files/:id
  body: none
  auth: bearer
}

params:path {
  id: 
  ticketId: 
}

auth:bearer {
  token: {{token}}
}
