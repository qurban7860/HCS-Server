meta {
  name: delete Ticket File
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/tickets/:ticketId/files/:id
  body: none
  auth: bearer
}

params:path {
  id: 
  ticketId: 
}

auth:bearer {
  token: {{token}}
}
