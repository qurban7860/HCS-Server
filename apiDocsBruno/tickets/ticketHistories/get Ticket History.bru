meta {
  name: get Ticket History
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/tickets/:ticketId/history/:id
  body: none
  auth: bearer
}

params:path {
  id: 
  ticketId: 
}

auth:bearer {
  token: {{token}}
}
